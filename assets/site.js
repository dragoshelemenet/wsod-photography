document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('[data-compact-carousel]').forEach(carousel=>{const slides=[...carousel.querySelectorAll('[data-slide]')],prev=carousel.querySelector('[data-carousel-prev]'),next=carousel.querySelector('[data-carousel-next]');if(!slides.length)return;let active=0,timer=null;const interval=Number(carousel.getAttribute('data-interval')||'3200');const normalize=v=>((v%slides.length)+slides.length)%slides.length;const rel=i=>{let d=i-active;if(d>slides.length/2)d-=slides.length;if(d<-slides.length/2)d+=slides.length;return d};const render=()=>{slides.forEach((slide,i)=>{const d=rel(i);slide.classList.remove('is-active','is-prev','is-next','is-far-left','is-far-right','is-hidden');if(d===0)slide.classList.add('is-active');else if(d===-1)slide.classList.add('is-prev');else if(d===1)slide.classList.add('is-next');else if(d===-2)slide.classList.add('is-far-left');else if(d===2)slide.classList.add('is-far-right');else slide.classList.add('is-hidden')})};const go=i=>{active=normalize(i);render()};const stop=()=>{if(timer){clearInterval(timer);timer=null}};const start=()=>{stop();if(slides.length<=1)return;timer=setInterval(()=>go(active+1),interval)};prev?.addEventListener('click',()=>{go(active-1);start()});next?.addEventListener('click',()=>{go(active+1);start()});slides.forEach((slide,i)=>slide.addEventListener('click',()=>{go(i);start()}));carousel.addEventListener('mouseenter',stop);carousel.addEventListener('mouseleave',start);carousel.addEventListener('focusin',stop);carousel.addEventListener('focusout',start);render();start()});document.querySelectorAll('[data-filter-root]').forEach(root=>{const chips=[...root.querySelectorAll('[data-filter]')],scope=root.closest('[data-filter-scope]')||document,cards=[...scope.querySelectorAll('[data-photo]')];chips.forEach(chip=>chip.addEventListener('click',()=>{const filter=chip.getAttribute('data-filter');chips.forEach(item=>item.classList.remove('is-active'));chip.classList.add('is-active');cards.forEach(card=>{const loc=card.getAttribute('data-location');card.hidden=!(filter==='all'||filter===loc)})}))});const cards=[...document.querySelectorAll('[data-photo]')],lightbox=document.getElementById('lightbox'),img=lightbox?.querySelector('.lightbox-image'),caption=lightbox?.querySelector('.lightbox-caption'),close=lightbox?.querySelector('.lightbox-close');const open=card=>{if(!lightbox||!img||!caption)return;img.setAttribute('src',card.getAttribute('data-lightbox')||'');img.setAttribute('alt',card.getAttribute('data-alt')||'');caption.textContent=card.getAttribute('data-caption')||'';lightbox.classList.add('is-open');lightbox.setAttribute('aria-hidden','false');document.body.style.overflow='hidden'};const shut=()=>{if(!lightbox||!img||!caption)return;lightbox.classList.remove('is-open');lightbox.setAttribute('aria-hidden','true');img.setAttribute('src','');img.setAttribute('alt','');caption.textContent='';document.body.style.overflow=''};cards.forEach(card=>card.querySelector('.thumb-button')?.addEventListener('click',()=>open(card)));close?.addEventListener('click',shut);lightbox?.addEventListener('click',e=>{if(e.target===lightbox)shut()});document.addEventListener('keydown',e=>{if(e.key==='Escape')shut()})});
